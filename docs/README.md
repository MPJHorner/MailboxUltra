# Screenshots

Drop a `screenshot.png` here (1600×1000 recommended) and the README + docs site hero figure will pick it up automatically. The site builder copies it to `dist/img/screenshot.png` at build time.
